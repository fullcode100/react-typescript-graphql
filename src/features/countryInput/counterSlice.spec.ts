import counterReducer, {
  CountryInputState, countryInputSlice
} from './countryInputSlice';

describe('counter reducer', () => {
  const initialState: CountryInputState = {
    value: '',
    status: 'idle',
  };
  // No tests for now
});
